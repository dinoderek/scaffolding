APP_ENV=local
APP_NAME=scaffolding-backend

# `./scripts/worktree-setup.sh` links `supabase/functions/.env.local` to
# `~/.config/boga/edge-functions/env.shared` for shared machine-level function
# identity defaults.
