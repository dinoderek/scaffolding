# Future Features Backlog

| Application area | Feature name | Brief description |
| --- | --- | --- |
| session list | view session | when clicking on a completed session we should show a view of that session |
| completed-session edit | autosave validation UX | improve autosave UX for invalid in-progress time inputs (start/end) so users get clearer save-state feedback and recoverability while text fields are temporarily invalid |
